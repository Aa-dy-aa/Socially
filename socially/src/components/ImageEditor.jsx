import React from "react";

const ImageEditor = ({ onClose, previewURL, settings, setSettings }) => {
  return <div className="fixed w-screen h-screen left-0 top-0 bg-black bg-opacity-75 z-10 flex items-center justify-center">
    <div className='bg-black rounded-xl p-12 flex flex-col gap-4'>Test</div>
  </div>;
};

export default ImageEditor;

